---

# (c) Wong Hoi Sing Edison <hswong3i@pantarei-design.com>
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

# Ceph version.
ceph_release: "16.2"

# Sets the number of replicas for objects in the pool.
ceph_osd_pool_default_size: "{{ (groups['ceph_osd'] | length >= 3) | ternary(3, 1) }}"

# Sets the minimum number of written replicas for objects in the pool in order
# to acknowledge a write operation to the client.
ceph_osd_pool_default_min_size: "{{ (groups['ceph_osd'] | length >= 3) | ternary(2, 1) }}"

# Ceph Object Storage Daemon configs.
ceph_osd_config:
  - { state: "present", section: "osd", option: "bluefs buffered io", value: "true" }
  - { state: "present", section: "osd", option: "bluestore cache autotune", value: "true" }
  - { state: "present", section: "osd", option: "bluestore cache size", value: "536870912" }
  - { state: "present", section: "osd", option: "osd disk threads", value: "2" }
  - { state: "present", section: "osd", option: "osd map cache size", value: "256" }
  - { state: "present", section: "osd", option: "osd max write size", value: "512" }
  - { state: "present", section: "osd", option: "osd memory base", value: "268435456" }
  - { state: "present", section: "osd", option: "osd memory cache min", value: "134217728" }
  - { state: "present", section: "osd", option: "osd memory target autotune", value: "true" }
  - { state: "present", section: "osd", option: "osd memory target", value: "536870912" }

# Devices to be used as OSDs.
# Device discovery is based on the Ansible fact 'ansible_devices'
# which reports all the devices on a system. If chosen, all the disks
# found will be passed to ceph-volume lvm batch. You should not be worried on using
# this option since ceph-volume has a built-in check which looks for empty devices.
# Thus devices with existing partition tables will not be used.
ceph_osd_volume: >-
  {%- set _ns = namespace() -%}
  {%- set _ns._params = [] -%}
  {%- for device, value in ansible_devices.items() -%}
  {%-   set _ns._params = _ns._params + [{ "objectstore": "bluestore", "data": "/dev/" + device }] -%}
  {%- endfor -%}
  {{ _ns._params }}

# Ceph OSD Pool to be created.
ceph_osd_pool:
  - { name: "rbd", application: "rbd" }
  - { name: "cephfs_metadata", application: "cephfs" }
  - { name: "cephfs_data", application: "cephfs" }
